// const initializePayment = require("./paymentController");

// const clear = async (req, res) => {
//   const { amount, productId, fullname, email } = req.body;

//   await initializePayment(am);
// };
